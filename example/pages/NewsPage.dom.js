import React from 'react';

import {TimeComponent} from '../views/TimeComponent';

export default {
    view: [
        {
            containerId: 'time-root',
            view: TimeComponent,
        }
    ],
};
