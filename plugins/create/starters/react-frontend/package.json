{
  "scripts": {
    "dev": "reframe dev",
    "production": "npm run build && npm run start",
    "build": "reframe build",
    "start": "reframe server",
    "deploy": "reframe build && reframe deploy",
    "reframe": "reframe"
  },
  "dependencies": {
    "@brillout/fetch": "^0.1.0",
    "@reframe/react-kit": "0.1.14",
    "@reframe/cli": "0.1.14",
    "react": "^16.3.2"
  },
  "name": "_starters-react-frontend",
  "version": "0.1.14",
  "private": "true",
  "checkDeps": {
    "skip": true
  }
}
