{
  "scripts": {
    "dev": "reframe dev",
    "production": "npm run build && npm run start",
    "build": "reframe build",
    "start": "reframe server",
    "deploy": "reframe build && reframe deploy",
    "reframe": "reframe",
    "docs": "mdocs"
  },
  "dependencies": {
    "@brillout/fetch": "^0.1.0",
    "@reframe/cli": "0.2.1",
    "@reframe/react-kit": "0.2.1",
    "react": "^16.3.2"
  },
  "name": "_starters-react-frontend",
  "version": "0.2.1",
  "private": "true",
  "checkDeps": {
    "skip": true
  },
  "devDependencies": {
    "@brillout/mdocs": "^0.1.19"
  }
}
